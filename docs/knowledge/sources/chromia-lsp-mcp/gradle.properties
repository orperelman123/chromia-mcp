# Placeholder for local builds only. Releases are tagged, and the deploy job passes the tag in as
# -Pversion, so this line is never bumped.
version=0.0.0-dev
org.gradle.parallel=true
kotlin.daemon.jvmargs=-Xmx2g
kotlin.code.style=official
