{
  "expectMin": 1691,
  "verifiedBy": "main 758d05d, the round-20 merge gate - 1691 tests, zero skips, zero errors, zero failures of our own, one proven upstream warning, fourteen partitions on 2026-09-10 03:27-04:15 with the run dated from the marker's first of fifteen invocations (GOAL.md, round 20); raised by hand because the partitioned chain tallies the union with --expect-min and loop-gate.mjs ratchets only its own full run",
  "verifiedAt": "2026-09-10",
  "why": "THE FLOOR THE GATE READS, so that nobody has to remember --expect-min. scripts/gate-tally.mjs reads this file whenever it classifies the repository's own app/build/test-results/test, and fails the run when fewer tests than expectMin executed - which is the only check between `tests === 0` and a suite silently narrowed to one test by a --tests filter that matched almost nothing or by a class that failed to compile. Adversary round 20, section 5, measured a ONE-test run passing every check CI makes. The number is a RATCHET: scripts/loop-gate.mjs rewrites it upward after a green FULL run and never downward, so it tracks the suite without a human step; lowering it is a deliberate edit that has to be argued for in the diff, because a floor that drops on its own excuses the narrowing it exists to catch."
}
