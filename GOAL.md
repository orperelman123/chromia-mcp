# The goal

**This MCP must make it as easy as possible for an agent to build secure Chromia dapps and chains — and the dapps it produces must be ones an auditor cannot drain.**

Everything else in this repo serves that. When a change is worth making, it is because it moves this; when a claim is worth doubting, it is because it only appears to.

## The bar: zero exploits

**A dapp built from this MCP must contain no exploit an auditor can land. Zero — not "fewer than last round".**

That is a number a round can fail, and most rounds have. It is not "the gate returned `ok:true`" — that is a linter result, and on 2026-09-02 it certified four agent-built dapps of which two were trivially drainable. The test is always:

> An agent builds a dapp using **only** this server's guidance, templates and gates. An independent hostile auditor then attacks it. Whatever they can drain is our failure, not the agent's.

**A round passes only when the auditor reports that they could drain nothing.** Anything less is a red round, however much coverage moved. Two things follow from that, and both have already bitten:

- **A drain in a class we have no template for is our failure too.** Every un-templated class attacked so far has drained. "We never claimed to cover streaming" is not a defence — the agent asked this server how to build, and this server answered.
- **The prose is part of the attack surface.** Round 7 drained a build whose author followed the template header exactly: the header prescribed a holding period against a step in pool value, and the attack was exit-only, so the advice was the vulnerability. A header sentence that is wrong is a defect of the same kind as a missing guard, and the residual list — where an auditor places the most trust — is the worst place to be wrong.

Run it as a round: build, attack, pin what got through, fix, re-attack. `app/src/test/resources/exploit-corpus/` is the standing scoreboard — every exploit found becomes a sample with a pinned verdict, so coverage is measured on every build instead of claimed in a report.

## Four principles, in priority order

1. **Guidance must not manufacture bugs.** A template that teaches an insecure pattern outranks any missed detection: it *creates* the hole. Defaults are what agents copy, so the default must be the safe one. (The scaffold once shipped `add_auth_handler(flags = [])` as the golden pattern; FT4 resolves flags with `contains_all()`, and `contains_all([])` is always true, so every value-moving operation copied from it silently dropped the Transfer flag.)
2. **Detection must not be evadable.** A rule keyed on anything the attacker chooses is not a boundary — key on **type and use, never on names**, and attack your own rule before shipping it. (`authorization-not-bound-to-caller` once matched a parameter-name allowlist: HIGH as `from`, silent as `victim`, same drain. The same evasion class then appeared three more times, and was finally systemic in a shared helper.)
3. **The gate must not cry wolf.** A gate agents route around is worse than no gate. A rule that cannot be made precise is **dropped, with the reasoning stated** — not shipped noisy. Undecidable properties (conservation, quorum) stay MEDIUM advisories and never make `ok:false`.
4. **What cannot be caught statically needs a different answer** — a template that makes the bug unwritable, a test the scaffold ships, or a runtime check. Never a regex pretending. The `governance`, `vault` and `staking` templates exist because their exploits are undecidable from syntax; each ships the original drain as a must-fail test.

## Never claim more than is true

`ok:true` reads to an agent as an audit pass. The notes must state the boundary, gaps must be named rather than rounded up, and a lane stays red rather than being closed by softening its assertion. **A fake green is worse than a red** — every one this project shipped came from evidence that looked like proof: a cached build that ran nothing, an empty results directory read as zero failures, a mutant that "went red" because the chain failed to start, a rule that passed every test its author wrote.

`scripts/loop-gate.mjs` is that discipline as a command. Use it instead of reading a build log.

## Where things stand

**Not met.** Fifteen templates ship, and the record is still the same shape it has been since round 1: **every un-templated class attacked has drained** — nine rounds, nine classes — and four *shipped* templates now carry recorded drains (lending twice, staking, governance). Every one of those was certified `ok:true` with zero findings and every conservation invariant exact. The scoreboard is `app/src/test/resources/exploit-corpus/CORPUS.md`; read it rather than this paragraph.

Two findings outrank any single drain, and both were in the gate itself:

- **The gate disarmed on every real project.** Adding the `@test` module the scaffold ships - and `run_rell_tests` requires - reclassified `main` as test surface, downgraded every production HIGH to advisory and flipped `ok` to true. It was armed only for a `main.rell` submitted with no tests, the one shape a real project never has. Fixed, pinned in three shapes including the rename evasion.
- **An object field write was not a mutation.** `config.fee = f` matches none of `create|update|delete`, so an unauthenticated operation whose only write was an object assignment drew no finding - and the default template taught exactly that spelling. The corpus row for the class was `CAUGHT` the whole time, on the *other* spelling. **A corpus row proves the sample is caught, not the class.**

The drains that were not missing guards were wrong sentences. Rounds 7, 8 and 9 were each topped by a defect in our own prose - a prescribed holding period that *was* the vulnerability; a claim that a monotone counter "can never rewrite the past"; a seam citing a victim's 144 bps execution loss as the pool's 79 bps reserve movement. Three of the defects found on 2026-09-03 were introduced that same day while fixing others. A green suite cannot tell whether a sentence is true, so the arithmetic a header cites is now recomputed *in a test* from the template's own constants, and the honest rule is: **every claim in the guidance is either testable or deleted.**

Closed on 2026-09-05, each with a replay and a mutant where a guard changed: round 9's three prose drains - the AMM's "more than 1500" is stated as a floor with no ceiling and the seam no longer tells authors to re-quote into a moved pool; the lending residual's "only against an already-insolvent position" was false because a liquidation *created* insolvency below 110% backing, so `liquidate()` now refuses to leave a solvent position insolvent and pays an under-water one value for value with no bonus; seam 2 now says a position is not transferable before it matures, since the confederate only ever had to pay. Also closed: the AMM's first-depositor "never a silent haircut" claim (a deposit must be redeemable for what it deposited, to a unit of rounding) and the marketplace's miscounted guards. Round 10, found by reading the governance header, is closed in the template with replay and mutant.

**The discipline is now a tool.** `verify_guards` runs the mutant check on an agent's own dapp: a guard is reported `load_bearing` only if its must-fail test passes with the guard and fails without it *because the attack landed*; a test that stays green without its guard is reported `vacuous`, and every environmental or wrong-reason failure is named rather than counted. It is step 4 of the agent loop, and it is attacked the round it ships, because a false verdict from a verification tool is a finding of the same rank as a drain.

**Round 11 (2026-09-06) failed twice over, and the second failure was the tool.** `verify_guards` gave six wrong verdicts in its first round, four of them `ok:true` - it searched raw text where comments live, replaced every copy in a file, classed a header-less test sibling as production, let `alsoRemove` carry a vacuous guard on a real one's back, and let a caller's fragment outrank the guard's own refusal. Each is closed and each probe is now a test (`VerifyGuardsProbeTest`) pinned to the true verdict: a tool-verdict finding is pinned as a test, and every verification tool ships with its own `*ProbeTest`. The round's two template drains are closed the same way, each with a replay and a mutant that reddens it because the attack lands: governance registration now mints NO voting weight (weight is a one-time founder-countersigned genesis allocation, capped, and the window closes once the DAO has stake), a proposal's quorum is fixed from the stake that existed when it was created and each voter's weight is a snapshot taken then - so neither a free grant nor a two-point stake after voting closes can move the bar; and the stablecoin's `liquidate()` refuses while the system is under-backed at the fresh price, before or after the seizure, so settlement is the only exit and the shortfall is shared in any order. The lane that wrote those fixes stalled on its own gate; the orchestrator committed the work, found its "verification run" was a targeted build rather than the gate, and gated the merged tree on main before pushing. **The lesson of round 11 is that verification tools are attack surface**: `verify_guards` lied six times in its first round, and an agent would have trusted every one.

**Round 12 (2026-09-06) attacked the tool the round after its fix, and got three more false verdicts out of the one input round 11 left alone.** Round 11 guarded the SEARCH and the ERROR; nothing guarded the SUBSTITUTION. A replacement that refuses in its own words was read as the attack landing (`ok:true`); a replacement of `/*` commented the real guard out and credited a vacuous one with the drain (`ok:true`); an `alsoRemove` entry that contained the guard stripped it in the control run and called a load-bearing guard vacuous. All three are pinned as probes (`p7*`-`p9*`) and closed by refusing the input instead of guessing: the replacement's literals count as refusals, the masked file must be identical outside the replaced span (`replacement_rejected`), and `alsoRemove` must be disjoint from the guard (`also_remove_overlaps_guard`). The general lesson is sharper than round 11's: **every input of a verification tool is attack surface, not only its outputs** - a fix that hardens two of three inputs has told the next round exactly where to look. The round's template drains are closed the same way, each with the drain as a must-fail test and a mutant that reddens it because the attack lands. Governance: `committed_treasury()` had reserved the treasury for a REJECTED proposal, so one point of stake out of 2001 froze it through a 2000-to-1 vote; now only APPROVED proposals inside their window reserve against everyone, a proposer's own undecided proposals reserve against that proposer alone, an identical re-proposal is refused while the original is live, and the guarantee round 10 rested on is arithmetic - a proposal records `funded_at_creation` and executes only if `paid_total + amount` fits inside it. Its genesis window had closed on the first member's stake, so one hand held 100% of the weight; now only the founder's signed `close_genesis()` shuts it, the DAO is either in GENESIS (claims, no stake) or LIVE (stake, no mint), and the header says the cost out loud: a founder who never closes it leaves a DAO that can never stake. Stablecoin: the round-11 refusal was a floor at 100% while the bonus is 105%, so the `[100%, 105%)` band still moved 12 tokens on transaction order (101/117/81 against 89/124/86); the floor is now the BONUS - backing must clear 105% before and after a seizure - and the 47.00 fixture settles 89/124/86 in either order; capping the payout at par instead was measured and rejected (97/117/85). And the un-templated order book, built from this server's own two sentences and drained because a partial fill's delete-and-recreate reset the maker's cancel clock (1000 kept where an uninterrupted maker keeps 2000), is the ELEVENTH template, `exchange`: terms immutable, one monotone `filled` counter so a row is never recreated and `created_at` is written once by the maker, the book matches (there is no `fill_order(id)`), and a crossing order fills at the resting price. Guard counts in headers are now measured by a test, because the stablecoin header said "Six" above seven guards. **Still open from round 12:** the stablecoin's `settle()` is permissionless and takes a HEALTHY position's collateral into a pool paying under par while that debtor could have burned at par and withdrawn in full - measured at 48.00 as 111/88 settle-first against 100/100 burn-first (`adversary-round12/attacks/stable_r12b.rell`); it is not yet a corpus row and not yet fixed.

**Round 13 (2026-09-06) failed twice over again, the round after the exchange shipped and the round after the tool's second fix.** `verify_guards` gave two more false `ok:true` verdicts, and the shape is the one rounds 11 and 12 should have predicted: each of those fixes read ONE MORE source of refusal messages (the guard's literals, then the replacement's) and left the caller's `attackLanded` fragment free to match any refusal from anywhere else - a second `require()` on the next line (defence in depth, which is exactly what a careful author writes), or a bound that lives in `module_args` and appears in no file at all. The fix is finally structural rather than an ever-longer list: **a refused transaction is never the attack landing.** The runner's own `Operation '...' failed` shape and every production file's string literals are refusals, read from the chain's half of a `run_must_fail` mismatch only, and the caller's fragment is consulted for what remains - a test-side assertion. `p11`/`p12` are pinned green; five other inputs (case-colliding paths, `..`, duplicate test names, a `module.rell` retyping the guard's module, BOM + non-ASCII) were attacked and held (`p13*`). The template drains are closed the same day, each with the drain as a must-fail test and a mutant that reddens it because the attack lands (the fixes follow the lesson): governance, where paying a member by proposal mints voting weight out of the treasury - `execute_proposal` retires no stake and there is no unstake - so 1000 allocated points became 4000 of weight and took 2000 of honest stake against a unanimous NO (bar 3500 from 7000), invited by the header's own "to give somebody weight after genesis, pay them by proposal"; the stablecoin's `settle()` taking a healthy debtor's par exit (111/88 against 100/100 at 48.00, the round-12 open item, now a row) and `withdraw_collateral` reading only the position while every other value-moving operation reads the system, so a whale withdraws 99 of 100 tokens and settles in the same block from 149% backed; the new exchange's `best_resting()` being an unfiltered `order @* {}` - the header's "index price and side" cannot be followed and the same ten trades took 1.8s / 7.4s / 26.3s behind 0 / 50 / 150 dust rows and were abandoned behind 300, one free registration paying for 10,000 rows; and recurring pull billing, built from the `streaming` redirect with every carryable guard carried, drained because the one guard that template rests on - PREPAID - has no analogue in a pull model, so `cancellable = false` became an irrevocable uncapped claim that took all 9,990 points the payer held, and the period boundary is a whole-fee step where the streaming header promises "one unit". The twelfth template, `subscription`, is the answer to the last two. Three prose defects were measured too - the governance claim that "two approvals are never sold the same money" (both are; the second is refused only at execution), a `GENESIS_POINTS` comment still describing round 12's drain as current behaviour, and the exchange's un-followable index advice. **The lesson of round 13**: a verification tool's verdict must come from the STRUCTURE of the evidence, not from a growing list of exceptions - every list has a next item, and the adversary finds it the round after it is written.

The round-13 fixes: governance treats the treasury and the voting weight as ONE pocket - a payout retires the stake that backed it, pro rata across stakers in a canonical order, so `dao.total_stake == dao.treasury_balance` at every block and total weight can never pass what genesis allocated (the four-turn ratchet leaves 1000 of weight, and the 2000-point vote loses 1000 to 2000 against a bar of 1500); the stablecoin's `withdraw_collateral` clears the same bonus floor `liquidate()` does, at a fresh price, with no exception for a debt-free position, and settlement is two calls a `SETTLEMENT_WINDOW_MS` apart during which only the two backing-RAISING operations run, so a debtor's par exit is available in either order (54 out / 46 kept both ways, where round 13 measured 111/88 against 100/100) and a whale's 99-token withdrawal is refused; the exchange's matcher selects with a where-clause on side, price and the taker's own id served by `index is_buy, price`, and `MAX_RESTING_ORDERS = 20` bounds the book per trader, pinned by a shape test rather than a stopwatch; and the twelfth template, `subscription`, makes the merchant's whole claim the escrow the payer funded, accrues the fee pro rata so no boundary is worth straddling, and lets either party cancel at any time - there is no `cancellable` term, and the header says why one cannot exist in a pull model. **Open from round 13:** the static rule `unbounded-voting-period` is keyed on a parameter NAME (`period|window|duration`) and fired MEDIUM on the subscription template's `amount_per_period`, a money parameter - principle 2 in reverse, an advisory on correct code; it is not yet a corpus row and not yet fixed. And round 13's own evidence carries arithmetic slips the fix lane caught (a 98-token probe described but not coded in `stable_r13.rell`, `203 * 48` where the code withdraws 99 of 300, and a "400-row" tier that the shipped fixture tops out at 300) - the corpus citations now say 300, the attack-file comments are left as the round wrote them.

**Round 14 (2026-09-06) failed on all three counts - five template drains, a DoS, a drained bridge, four static rules firing on correct code, and three more false `verify_guards` verdicts, this time in the conservative direction.** Round 13's structural rule read ANY refusal as "the attack did not land"; round 14 showed a refusal from a LATER operation after the attack landed, a test-side `require()` in production words, and a diagnostic string carrying its own `but was <...>` - each turned a genuinely load-bearing guard into `still_refused`, and an author's fix for that is to weaken the test until the tool likes it. The rule now compares the runner's failing frame with the declaration the guard lives in: a refusal from the guard's own operation is `still_refused`; anything else red on a test that was green with the guard is the damage being noticed, and no string literal is consulted at all. `p14a`-`p14c` pinned green; `p14d`/`p14e` held. The template drains are closed, each with the drain as a must-fail test and a mutant that reddens it because the attack lands (the fixes follow this paragraph): the subscription's time ceiling derived from CURRENT funding (a top-up pays for eleven unfunded months in one block, 10000 taken) and its "one unit" staircase that is a block's worth of fee (2778 units on the largest fee and shortest period); the exchange's per-ACCOUNT self-dealing rule while registration is free (a second key cancels inside `MIN_RESTING_MS`, 500 points off an honest maker) and market-priced dust on free registrations (2.8s -> 16.6s behind 100 rows); governance retirement rounding UP in sort order so the first owner pays every rounding point (a two-point payout takes 100% of a one-point staker); the stablecoin's debt-free depositor frozen in the bonus band, the failing position voiding the settlement opening in the block it acts, and one honest post inside the window moving 7 of 71 surplus tokens; and a bridge receiver built from the `ft4` redirect - "copy the transfer-conservation test", which is structurally blind to a mint - paying one burn ten times (10000 from 1000) and three accounts 256000 against one burn: the thirteenth template, `bridge`, is the answer. **Four static rules key on identifier NAMES** (`unbounded-voting-period`, `majority-without-quorum`, `query-returns-secret-data`, `value-sink-without-withdrawal`): renaming one identifier silences each, and the first had already written `MIN_FEE = 1` into the shipped subscription template to keep the gate quiet - principle 2 in reverse, pinned as four `FALSE_POSITIVE` rows and in a lane to be rebuilt on structure.

The round-14 fixes: the subscription's funding buys time FORWARD from the moment it arrives (never time already passed), and the boundary step is bounded at one unit per second of the period (the runner's ten-second block is a ten-unit step, where round 14 measured 2778); the exchange's resting order is a commitment whoever fills it (a maker's proceeds inside `MIN_RESTING_MS` stay locked, so a second key buys nothing) and the book is bounded by `MIN_NOTIONAL` - something an attacker pays for - rather than by free registrations; governance retirement is pro rata by largest remainder, so the odd point moves and no staker is emptied by a payout smaller than their stake; the stablecoin's backing is the DEBTORS' collateral, so a debt-free depositor withdraws regardless of others' debt, and a settlement opening is resolved once, at its window, never voided in the block the failing position acts; the thirteenth template, `bridge`, keeps a processed-burns registry that is a database key, holds every attestation to the burn it opened by a `require` pair, mints only at the relayer threshold, and counts minted units against attested burns; and the four name-keyed rules are rebuilt on structure with a rename-invariance test - the four `FALSE_POSITIVE` rows are `CLEAN`. **What this round taught about the loop itself:** neither fix lane ever got a build slot, and the gate on the merged tree found five defects they shipped blind (a compile error, a wrong mutant, a query-clock pin, a redirect pin for a name that had become a template, a flag regex) plus one of the orchestrator's own - the first version of the timestamp exclusion used the clock-fed DATA-FLOW set and hid the round-4 mint behind ten reds; the exclusion is the DECLARED type only. Two gates were killed at the 45-minute cap by memory contention with 3.6 GB free on a 32 GB box, with their results deleted; the cap is 60 minutes and the build slot is written down as a memory rule. Also shipped: the MCP Kotlin SDK 0.7.7 -> 0.15.0 with Streamable HTTP at `/mcp` beside SSE, a `public` tool profile pinned by a compile-time marker (a strategy that does not say whether it touches the machine does not compile), and a Host allow-list on both endpoints; and the first **agent-experience audit** - a read-only lane driving the jar exactly as a fresh agent would - measured ten defects that no adversary round had seen: the docs index depended on the process's working directory (a registered install silently served a 320-day-old index), `search`/`fetch` never said so, the onboarding text claimed human blockers for steps this server automates, the flagship `ft4` template's own tests went red on the honest first run, `write_deployment_config` regenerated a gutted `chromia.yml` that `validate_chromia_yml` blessed, eighteen plausible aliases scaffolded `hello` with `isError:false`, every scaffold carried the same 22 KB of notes, and three names for the Rell sources. Those fixes are on a branch with a test built from each audit call, gated next. The QA loop is now the third loop this repository runs, and its bar is the same: a measurement, pinned as a test, or nothing.

**Round 15 (2026-09-06) failed on every count but one.** `verify_guards`, attacked at the FRAME the fourth fix introduced, gave four false verdicts: a guard inside a `function` was read as refused by any operation (the short-circuit `guardInFunction`), the module prefix was thrown away so `audit:take` counted as `main:take`, the guard's own operation refusing in a LATER transaction was read as the attack being refused (the frame says which declaration, never which transaction), and a query-side guard that is only defence in depth was certified `load_bearing` with `ok:true` - the first verdict in the dangerous direction since round 11, because a failing query carries no `Operation` frame at all and the comparison was simply skipped. The thirteenth template, `bridge`, drained three ways on its first round: the period cap is a fixed window anchored on the opening mint (200000 crossed a 100000 cap between T0+DAY and T0+DAY+2), the first attestation binds recipient and amount so one relayer below threshold froze a 100000 burn forever, and the operator enrols every relayer so one key minted the whole 1000000 cap against fabricated burns with the invariant exact throughout. Three template claims are false with numbers: governance's "no payout takes a staker's whole holding unless it takes everyone's" (501 of 1001 retired 50.0% of the 1000-point staker and 100% of the 1-point staker), the stablecoin's "burn_stable raises backing" (a full par exit inside a pending settlement took backing from 99.3% to 72.0%), and the exchange's "fifty bid rows" (a hundred, because `MIN_NOTIONAL` is priced for the sell side only). The rules lost five drains and gained a false positive: the round-14 timestamp exclusion is keyed on a BARE NAME over the whole submission, so a balance declared `timestamp` mints unseen and a decoy time field named `balance` elsewhere silences the rule for every balance; an upper bound alone silences `unbounded-voting-period` while `period = 1` is accepted; a floor of two silences `majority-without-quorum`; `query-returns-secret-data` fires on an existence check over an unrelated secret-named row and misses a secret returned through a one-line helper. The ninth un-templated class drained: "an OTC swap escrow between two parties with a timeout" routes to `template=amm` on the word swap, and the immutable-row-deleted-whole pattern that answer teaches lets a one-unit fill reset the maker's timeout for ever (94 of 100 units still locked at hour six, then bought at the six-hour-stale price), with only one leg escrowed so the timeout is a free option. **The one target that passed cleanly is the `public` profile**: the four disabled tools are absent and gated, nothing reads a keystore or spends, the Host allow-list held against every spoof, `/health` leaks four fields, and the session cap answered 503 after 248 anonymous posts. The adversary lane also pinned no corpus rows and committed 749 vendored FT4 library files as "evidence" - both corrected before the merge; the rows are the fix lanes' first job. The lesson of round 15 is the lesson of rounds 11-14 said louder: **a verdict rule written as a patch on the previous rule is beaten by the next round at the seam of the patch** - the fifth `verify_guards` fix is designed from what the runner emits and what the tool knows, with an honest `ambiguous_refusal` where the structure cannot decide, instead of a sixth list.

The round-15 fixes, gated together at 1483/0/0/0: `verify_guards` resolves a guard inside a function through its CALLERS, compares frames module-qualified, separates a frame-less error by which declaration can produce its text (a `rell.test` assertion or a message only the test module owns is the damage being noticed; a message only a production query owns is a refusal), decides a repeated invocation of the guard's own operation by **differential truncation** - the mutant is re-run against the test cut after the first invocation, so the tool measures instead of guessing - and answers `ambiguous_refusal` (never `ok:true`) where none of that decides; the first version of that fix was over-conservative on four test-side probes, which is a finding in its own rank and is pinned. The rules key the timestamp exclusion per (entity, field) and only for pure clock arithmetic, demand a LOWER bound for a period, recognise a tally by type and use (a rename-invariance test holds every rule to that), and read a query's RETURN expression with helpers flattened. The bridge's cap is a rolling window of `mint_event` rows, relayers vote on the `(recipient, amount)` tuple with per-round attestations and a reopen after `ATTESTATION_WINDOW_MS`, set changes need the threshold of existing relayers and the operator can only pause, and a reopen binds its caller to the burn it moves. Governance's leftover point goes to the largest remainder among stakers the payout does not empty. The stablecoin's reserve is closed while an opening is pending (the sentence "burn_stable raises backing" is replaced by the measured 99.3% -> 72.0%, and both structural alternatives were measured to reopen rounds 13 and 14 and rejected in the header). The exchange has `MIN_ORDER_UNITS` beside `MIN_NOTIONAL`, so both sides are bounded and the header's numbers are the measured ten bids and twenty sells per grant. And the fourteenth template, `escrow`, escrows both legs at creation, settles all-or-nothing, keeps the offer revocable in any block, and lets either party close after the deadline. Also shipped since round 14: **the eight agent-experience audit fixes** (F1 F2 F3 F4 F5 F6 F7 F10), each pinned by a test built from the audit's own call - they were merged blind from a lane that never got a build slot and cost three compile rounds and 23 red tests before a lane with build access made them green. The infrastructure lesson of the day is written into memory and the lane briefs: the build slot is a memory rule, the WSL VM is capped, and a lane without a slot ships defects.

**Round 16 (2026-09-06) failed on all three counts, and the tool failure was systematic.** `verify_guards` gave seven false verdicts out of thirteen probes, six of them `ok:true`, from four defects: the fifth rule compared the runner's `main:take` frame with a module name the tool derived from the raw path, so under the `src/` layout every scaffold writes it read `src.main:take` and never matched - a second guard in the SAME operation, the ordinary defence in depth, was certified load-bearing on every real project while the probe suite, written with flat paths, stayed green; a test file called `main.rell` erased every production frame by basename; an `@extend` function was reached by nothing; and both the statement-order rule and differential truncation were blind to frame-less refusals and to a `for` loop. The first two were fixed the hour the report landed (modules derived the way the runner derives them, frames split by module; gate 1486/0/0/0) and the other two are in a lane whose brief is a design change rather than a sixth heuristic: **define the canonical test shapes the tool proves - a must-fail test and a must-hold test - prove exactly those, and answer `ambiguous_refusal` for everything else.** Three of the four rules rebuilt in round 15 were evaded one token away (a lower bound whose constant is 0; a quorum floor the proposer writes into the row; a secret returned through an accumulator), and the clock-difference mint is a plain gap. Governance's leftover point drained a third time (stakes 100/1/1, payout 51: one one-point holder retired to zero while the other keeps its point, because a zero-remainder staker can never take the point). The bridge has two denial levers: sixty-four units of dust fill the period's row cap and close the bridge for a day, and a reopen puts a round boundary between two honest votes so a threshold-reaching payment mints nothing. Eleven prose defects, headlined by the escrow guard "there is never a block in which one party has parted with value and the other has not" - false for the whole window, and the template's own green test asserts the counterexample. And the audit's closest-template routing sends "an investment DAO" to the STREAMING template because `has("vest")` matches the substring of "investment" before the governance branch runs; alias precedence silently discards a second alias with different content; `moduleArgs: {}` hands the run to the submitted yaml; a `deployments: # prod` line gets a second `deployments:` key appended. The lesson this round adds to the tool's: **a probe suite that shares one fixture layout is blind to the layout it does not use - the twelve rounds of flat-path probes proved the rule for a project nobody ships.** The round-16 fixes, gated together: `verify_guards` no longer classifies every red - it proves **two canonical test shapes** (a must-fail test whose one statement is a single-operation `run_must_fail` on the guard's declaration; a must-hold test whose one statement expects success and whose damage is then asserted), resolves callers through `@extend`/`@extendable`, derives modules the way the runner does, splits frames by module, and answers `ambiguous_refusal` (never `ok:true`) for loops, tables, several invoking statements, multi-operation transactions and helpers with several call sites - differential truncation is gone with the question it answered, and eight probes were re-pinned with their reasoning rewritten. Governance's leftover point goes to a staker who can afford it (three tiers; a zero remainder is a real candidate; 574,704 payouts brute-forced with zero violations). The bridge charges its row budget in VALUE (closing it now costs 98.4% of the period cap instead of 64 units) and a reopen MOVES a relayer's voice instead of erasing everyone's. Escrow's absolute is replaced by the measured asymmetry; the other ten prose defects are corrected and pinned. The redirect matches whole tokens (`vest*` is vesting, not in-vest-ment; "an investment DAO" reaches governance; "a loyalty programme" no longer reaches the AMM through progr-amm-e) and the vault's note is its own again. The rules resolve a bound's VALUE (a constant 0 is no bound; a module-arg bound must carry a positive default), refuse a floor the proposer writes into the row, follow a secret into an accumulator the query returns, and treat a clock difference as a quantity - five rows flipped to CAUGHT, scoreboard 52/102 caught. Two aliases with different content are an error; an explicit `moduleArgs` (even `{}`) is the whole set; the deployments key is found by parsing. And the **context-economics** lane landed: `prompts/list` and `prompts/get` are served natively with real tool names (90 prompts), every tool description is under a 1,200-byte bound with the long form in `describe_tool` and a sectioned `chromia_help` (worst section 6 KB), compact mode brings first contact from ~25k to **~11k tokens** (44,198 B), and the docs index ranks documentation ahead of the compiler's own sources (top hits on the ten audit questions: docs pages 4/10 -> 10/10; correct 3 -> 6). Main is 1539 tests green before that merge and gated again with it.

**Round 17 (2026-09-07) was red on three of four counts, and it also found what the test suite itself had been hiding.** `verify_guards` gave **ten false verdicts out of eighteen** shape probes - four dangerous `still_refused` answers where the truth was `ambiguous_refusal`, four honest one-statement tests refused, and two verdicts contradicting the tool's own long form - from two lines: the helper scan read ONE test file (a helper imported from another test module hid a second invocation, and an honest invocation written there was invisible) and the operation count ran over the reaching helpers only (a helper that quietly added a second operation read as a single-op shape). An un-templated **insurance pool**, built from this server's own redirect answer (`template=ft4`), drained twice on a running chain: a cancel refunded a premium the holder's own claim had already spent (alice 1000 -> 1300, reserve -100 with 2000 of cover still written; 2100 from 1000 when repeated) and an exit race moved 100 points on transaction order alone - `rell_security_check` said `ok:true` with zero findings on both. Two rule evasions: a participation floor of one hidden behind `function participation_floor(): integer = 1;` and a floor written by a second permissionless operation the proposer also signs. The honest fallback named "the ELEVEN hardened ones" and omitted `escrow` for five weeks while fourteen shipped. The fixes, gated together: `verify_guards` (the seventh design) resolves helpers across every test module through imports, wildcard imports and aliases, counts operations and `run_must_fail` over the statement's whole call closure, follows helper chains sixteen deep and refuses honestly past that, attributes a frame-less refusal to the mutant sources that ran (a replacement's own `require` counts), and documents `stillRefused`/`attackLanded` as SHAPE B ONLY with the measured reason - ten false verdicts to zero with no pin edited. The rules resolve a bound through a parameterless constant function (depth eight, cycle-safe) and treat a field any unauthenticated operation writes as proposer-controlled - scoreboard 55/107 caught. The **fifteenth template, `insurance`**: one helper returns every premium and returns it less what the policy was already paid, claims settle pro rata in rounds so transaction order moves nothing, cover is bounded by the reserve, and both drains ship as must-fail tests behind eleven guards with eleven mutants, all eleven `load_bearing` through the real server; the redirect's roster is now DERIVED from the template list with a build-time check, seven misroutes were re-decided (three now answer honestly with the missing guard named), and `docs/TEMPLATE-GAPS.md` carries seven measured rows. And the suite: an external review called "143 retained passing tests" no proof of a docs-only change, and it was right - the hand-picked reader list was wrong (six files read this document, not five) and only a derived one can be trusted, so `--docs-only` now derives its classes from the diff; the local `--allow-skip` bypass is gone and the gate refuses to run without the live environment; **every test double is gone** - the explorer, the docs site, the chain, the CLI and the embedder are exercised for real, `NoTestDoublesTest` scans the sources for zero, and pointing the explorer tools at the real explorer showed **four of them never worked** (`dashboardData` and `groupedTransactionsByBlockchain` answer `INTERNAL_ERROR`, node availability sits behind a reCAPTCHA) while each had a green unit test, so they are retired; the local chain had silently run the deprecated merkle hash version 1 because the key sat at the wrong level of the config; the docs say one true thing about transports; nothing tracked points at the machine it was written on (the repository is public). Main is 1623 tests with zero skips, zero errors and zero doubles, gated in fourteen partitions on a starved laptop; zero failures of our own, and one upstream warning, recorded under round 18.

**Round 18 (2026-09-08) was red on every count it attacked, the same day the round-17 fixes landed, and every finding is closed.** `verify_guards` (the seventh design) gave **seven false verdicts out of eighteen** - six dangerous - through three inputs the design still modelled instead of parsing: a Rell exact import (`import tests.helpers.{ audited };`) the import regex did not match, so a helper reached that way was invisible again; the transaction constructor form (`rell.test.tx(take(...), audit(...))`) with no `.op(` at all, counted as zero operations, and zero passed every branch of the shape check; and an import alias shadowing the guard's own module (`import main: tests.helpers;`), counted as a production invocation before the calling module's bindings were read. The **eighth design** parses every import form the compiler accepts (measured against chr 0.29.10 / rell 0.15.0 before a line changed), counts operations **structurally** over the statement's whole call closure with an unresolvable value answered `ambiguous_refusal` rather than zero, and resolves a qualifier in the calling module's own bindings first - seven false verdicts to zero, no pin edited, rounds 11-17's sixty probes still green. `rell_security_check` was evaded **six ways with the same number** (a default-valued parameter, `2 - 1`, `min(1, 5)`, a namespaced val, a stored floor read through a function, the same field read through a query - each one substitution from round 17's own sample) and missed a raffle that hands `op_context.last_block_time` to a helper whose body does the modulo; the fix stops naming shapes and **evaluates a bound's value** (literals, vals under any namespace, module_args defaults, arithmetic, `min`/`max`/`abs`, indexes, struct fields, text lengths, a `when` whose arms agree, any function or query whose body is one expression with unbound parameters taking their defaults, depth-bounded and cycle-safe), traces who writes a floor through functions and queries, and follows the block clock through a callable's parameters into the selector - seven corpus rows GAP to CAUGHT, **63/116 caught, 27/29 clean**, eleven further spellings pinned before the adversary could, the fifteen templates' honest clock uses still silent, and the deployment-floor DAO confirmed a non-finding. The **fifteenth template drained a third time**: a cancel filed inside an open claim round ran ahead of the settlement it was about to share (alice=1200 bob=900 eve=900 against the honest 1000/1000/1000, 200 points on transaction order); now an exit filed inside a claim round **joins that round's settlement** instead of racing it, twelve guards, the mutant reddens on the round's exact numbers, and the round-17 drains re-ran green. The redirect **laundered a mixed ask** ("a lending pool that also runs a weekly raffle" scaffolded `lending` with not one word about the raffle, and the raffle drained: trudy 100 to 400 over five draws while alice and bob went 1500 to 1350); an ask naming an uncovered class is now answered by that class FIRST, the covered half named by template, and nothing scaffolded; the raffle class heads `docs/TEMPLATE-GAPS.md` and every one of the fifteen templates ships an EXTENDING section naming its own seam (it was four). And the suite's own claims were attacked and held to the same standard: **six of eight test doubles were invisible** to a source-level scan, so the zero-doubles proof now reads the **compiled classes** - supertype closure, constant pool, `invokedynamic` descriptor - with the six evasions kept as a compiled-never-run source set the scan must catch and the seam list derived from the production API; a live test could pass on an upstream refusal (`get_asset_top_holders` stayed green through eight consecutive live `INTERNAL_ERROR`s), so a live helper now FAILS on one; every `prompts/get` argument is capped at 1,024 characters and control-character-free (worst case 15,316 B to 3,965 B); and the 90 s `run_rell_tests` bound was shown not to be an FT4 cost (a registration is about a second; the ~17 s around every `chr test` is the JVM, the compile and the schema), so the bound stands and the host requirement is written down. Main is 1623 tests, gated in fourteen partitions on a 15 W laptop, zero skips, zero errors, zero doubles, zero failures of our own. The suite also stopped lying in the other direction: a live test whose third party is proven down is now an **UPSTREAM WARNING** - still a failure in the XML, counted and named separately by the gate, never a pass - and it is proven only by an allowlisted signature the third party alone can produce plus a failed canary or a dated `docs/UPSTREAM.md` entry naming the query; today that is one test, `filter_blockchains` by chain state, because the explorer answers `allBlockchains(state:)` with `INTERNAL_ERROR` on every call (#3b), while its per-chain analytics, measured dead at 60 s in the morning, answered again by evening (#11 records both the recovery and that its cost is unbounded in chain size over an index stale since 2026-09-04). A warning is a red for the upstream, not for us; the ledger entry is the debt.

**Round 19 (2026-09-09) attacked the round-18 fixes and the suite's own proofs, failed on six counts, and every one is closed.** `verify_guards` (the eighth design) was beaten one builder and one namespace past round 18: a `rell.test.block()`'s own `.tx(op, op)` counted as **zero** operations (in shape B a false `ok:true` - the audit that refused was in the same transaction, so nothing persisted and nothing was measured), a helper inside a `namespace` was invisible because a namespace is not an import, and an honest one-operation test was refused because its operation was a helper **parameter**. The **ninth design** counts a block's carriers as well as the transaction's, resolves a qualifier through namespaces in five measured spellings as well as every import form, and binds a helper's parameters to the caller's arguments - four false verdicts to zero with no pin edited, rounds 11-18's seventy-two probes still green, fourteen new probes each baseline-green/mutant-red on a real chain, and twenty-one Rell spellings put in front of the compiler first so that two the fix might have modelled (`.ops(`, `.txs(`) are pinned as not existing. `rell_security_check`'s constant evaluator had **three silent floors** - `when { book.pot > 0 -> 0; else -> -1 }` (no common arm value, so unresolved, so the benefit of the doubt), and `limits_of().floor` read off a query and off a function (a call followed by anything was never valued) - and all three DAOs drained **1,000,000 to 0 with one signature and one ballot** on a running chain; a term the evaluator cannot value is now asked a second question, what is the BEST it can be, so a floor whose maximum is still 0 bounds nothing on every path and fires, and a struct field off a call is valued the way the `val` form always was - **7/7 MUST_FLAG caught, 0/11 MUST_STAY_CLEAN fired** (the ten correct DAOs stay silent, `1 / ZERO` stays clean because Rell has no value for it either), scoreboard **69/122 caught, 27/29 clean**. The **zero-doubles proof was walked past four times**, and each evasion was one of the scan's own definitions written down as a literal: a seam had to be a type production OWNS (a named class and a SAM lambda over langchain4j's `ContentRetriever` were invisible), the proxy factories were two names (`MethodHandleProxies.asInterfaceInstance` manufactured the same substitute), and the scanned root was the string `app/build/classes/kotlin` (a double written in Java, or checked in as bytes under `resources/test`, was proven absent by nothing); every literal is now a derivation - a seam is every interface or abstract class production's own bytecode names (two derived SAM targets became eleven, the test tree still at zero), manufacturing entry points are read off the constant pool as calls, and the scanned trees are the `test` task's own runtime classpath minus dependency artifacts and production's output, class files recognised by their magic, with both tests asserting that every classpath directory is either production's or scanned. The **UPSTREAM WARNING could be faked**: a ledger entry that does not exist bought the status because the gate never opened `docs/UPSTREAM.md`, and a file the producer did not write bought it because `FAILED_OTHER` counted as a failed canary; the gate now reads the ledger itself (the entry must exist, its heading must match exactly, the section must be dated and name the query), every evidence file is **bound to its run** by a digest the failing test writes into its own assertion message and the gate recomputes from disk, `FAILED_OTHER` proves nothing, and the canary got an **independent path** (a plain `java.net.http` client with its own bounds) so that our own request timeout, driven through the real seam at 1 ms, is classified RED end to end and not as the third party being down. The redirect was **laundered by a synonym** ("a tombola for token holders" drew `ok:true`, `template=ft4` and four guard-free files from the class this server declines by name); the un-templated classes are now recognised by **concept** in seven languages with the ask folded before it is tokenised, the refusal names the class and its missing guard even when nothing covered is named, and a twenty-five-ask clean-pass corpus proves `abetting` is still not `betting`. The three reviews the adversary did not reach were done as measurements: the fifteen EXTENDING sections resolve every identifier they name against a closed taxonomy (the insurance exit queue drains inside the settlement it joins), the seventy-one long forms are gated for cross-references, parameters and a stale Rell pin now tied to its constant, and the commit-reveal raffle has its design note at the head of `docs/TEMPLATE-GAPS.md`. The `prompts/get` cap held in every direction. Main is 1650 tests, gated in thirteen partitions on the same 15 W laptop, zero skips, zero errors, zero doubles, zero failures of our own, 1 upstream warning(s) proven by the widened gate.

What holds: each template makes its own exploit class unwritable rather than detected, and every guard has a mutant that reddens a shipped must-fail test *because the attack lands* - a mutant that goes red for any other reason proves nothing, and one was caught doing exactly that. `scripts/loop-gate.mjs` is the merge discipline as a command and `docs/CI.md` is the same gate as GitHub runs it - one classifier, `scripts/gate-tally.mjs`, for both, because two gates with different definitions of green is one gate and one bypass; `docs/ADVERSARY-ROUND-BRIEF.md`, `docs/TEMPLATE-GAPS.md` and `docs/AGENT-LANE-BRIEF.md` are the method, so a session can be picked up cold.
